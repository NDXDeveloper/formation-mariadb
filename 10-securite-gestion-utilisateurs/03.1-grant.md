🔝 Retour au [Sommaire](/SOMMAIRE.md)

# 10.3.1 GRANT : Attribution de privilèges

> **Niveau** : Avancé
> **Durée estimée** : 2 heures
> **Prérequis** : Section 10.2 (Création et gestion des utilisateurs)

## 🎯 Objectifs d'apprentissage

À l'issue de cette section, vous serez capable de :
- Maîtriser la syntaxe complète de la commande GRANT
- Attribuer des privilèges à différents niveaux (global, base, table, colonne, routine)
- Comprendre et appliquer les nouveaux privilèges granulaires de MariaDB 11.8
- Implémenter des stratégies de sécurité basées sur le principe du moindre privilège
- Diagnostiquer et résoudre les problèmes de permissions en production

---

## Introduction

La commande `GRANT` est le pilier du modèle de sécurité de MariaDB. Elle permet d'attribuer des privilèges aux utilisateurs et aux rôles selon une hiérarchie à cinq niveaux, offrant une granularité exceptionnelle pour contrôler l'accès aux données et aux opérations du serveur.

Le système de privilèges MariaDB repose sur le principe du **moindre privilège** (*least privilege principle*) : chaque utilisateur ne devrait disposer que des permissions strictement nécessaires à l'accomplissement de ses tâches. Cette approche minimise les risques en cas de compromission d'un compte.

---

## Syntaxe générale de GRANT

### Syntaxe de base

```sql
GRANT privilege_type [(column_list)]
    [, privilege_type [(column_list)]] ...
    ON [object_type] privilege_level
    TO user_or_role [, user_or_role] ...
    [WITH GRANT OPTION]
    [MAX_QUERIES_PER_HOUR count]
    [MAX_UPDATES_PER_HOUR count]
    [MAX_CONNECTIONS_PER_HOUR count]
    [MAX_USER_CONNECTIONS count]
    [MAX_STATEMENT_TIME time];
```

### Composants clés

| Composant | Description | Exemples |
|-----------|-------------|----------|
| **privilege_type** | Type de privilège accordé | `SELECT`, `INSERT`, `UPDATE`, `DELETE`, `ALL PRIVILEGES` |
| **object_type** | Type d'objet concerné | `TABLE`, `FUNCTION`, `PROCEDURE` |
| **privilege_level** | Niveau de granularité | `*.*`, `db.*`, `db.table`, `db.routine_name` |
| **user_or_role** | Destinataire du privilège | `'user'@'host'`, `role_name` |
| **WITH GRANT OPTION** | Permet de déléguer le privilège | - |

---

## Niveaux de privilèges

MariaDB organise les privilèges selon une hiérarchie à cinq niveaux, du plus général au plus spécifique.

### 1. Niveau Global (*.*)

Les privilèges globaux s'appliquent à toutes les bases de données du serveur.

```sql
-- Privilèges administratifs globaux
GRANT SUPER, RELOAD, PROCESS ON *.*
    TO 'dba_user'@'localhost';

-- Accès en lecture seule à toutes les bases
GRANT SELECT ON *.*
    TO 'readonly_global'@'%';

-- Privilèges de sauvegarde
GRANT RELOAD, LOCK TABLES, REPLICATION CLIENT ON *.*
    TO 'backup_user'@'backup-server.example.com';
```

💡 **Conseil** : Les privilèges globaux sont stockés dans la table `mysql.global_priv`. Utilisez-les avec parcimonie.

⚠️ **Attention** : `GRANT ALL PRIVILEGES ON *.*` accorde tous les privilèges sur toutes les bases, y compris les futures bases créées. À réserver aux super-administrateurs.

### 2. Niveau Base de données (database.*)

Les privilèges s'appliquent à toutes les tables d'une base spécifique.

```sql
-- Développeur avec droits complets sur une base
GRANT ALL PRIVILEGES ON production_db.*
    TO 'dev_user'@'10.0.%.%';

-- Analyste avec accès lecture seule
GRANT SELECT ON analytics_db.*
    TO 'analyst'@'%';

-- Utilisateur applicatif avec droits CRUD
GRANT SELECT, INSERT, UPDATE, DELETE ON app_db.*
    TO 'app_user'@'app-server.example.com';
```

💡 **Conseil** : Les privilèges de base sont stockés dans `mysql.db`. Ce niveau est optimal pour la plupart des applications.

### 3. Niveau Table (database.table)

Permissions spécifiques à une table donnée.

```sql
-- Accès à une table sensible uniquement
GRANT SELECT ON hr_db.salaries
    TO 'hr_readonly'@'%';

-- Permissions différenciées par table
GRANT SELECT, INSERT, UPDATE ON orders_db.orders
    TO 'order_manager'@'%';

GRANT SELECT ON orders_db.orders
    TO 'sales_viewer'@'%';

-- Empêcher la suppression de données critiques
GRANT SELECT, INSERT, UPDATE ON audit_db.audit_log
    TO 'app_user'@'%';
-- Pas de DELETE accordé
```

### 4. Niveau Colonne (avec liste de colonnes)

Le niveau le plus granulaire pour les données tabulaires.

```sql
-- Accès restreint aux colonnes non sensibles
GRANT SELECT (employee_id, first_name, last_name, department)
    ON hr_db.employees
    TO 'hr_viewer'@'%';

-- Accès en modification limité
GRANT UPDATE (salary, bonus)
    ON hr_db.employees
    TO 'payroll_admin'@'%';

-- Combinaison SELECT global + UPDATE spécifique
GRANT SELECT ON hr_db.employees TO 'manager'@'%';
GRANT UPDATE (performance_rating) ON hr_db.employees TO 'manager'@'%';
```

💡 **Conseil** : Les privilèges au niveau colonne sont stockés dans `mysql.columns_priv`. Vérifiez les performances pour de nombreuses colonnes.

⚠️ **Attention** : Les privilèges colonnes peuvent complexifier l'administration. Privilégiez les vues pour le masquage de données si possible.

### 5. Niveau Routine (procédures et fonctions)

Contrôle de l'exécution des procédures stockées et fonctions.

```sql
-- Autoriser l'exécution d'une procédure
GRANT EXECUTE ON PROCEDURE sales_db.calculate_commission
    TO 'sales_app'@'%';

-- Privilège sur toutes les routines d'une base
GRANT EXECUTE ON sales_db.*
    TO 'reporting_user'@'%';

-- Fonction spécifique
GRANT EXECUTE ON FUNCTION analytics.compute_metrics
    TO 'dashboard_app'@'10.20.30.%';
```

---

## Types de privilèges

### Privilèges de données (DML)

| Privilège | Description | Cas d'usage typique |
|-----------|-------------|---------------------|
| **SELECT** | Lecture de données | Applications, reporting, analyse |
| **INSERT** | Insertion de nouvelles lignes | Applications transactionnelles |
| **UPDATE** | Modification de données existantes | Applications CRUD |
| **DELETE** | Suppression de lignes | Gestion de données, purges |

```sql
-- Utilisateur applicatif standard (CRUD complet)
GRANT SELECT, INSERT, UPDATE, DELETE ON app_db.*
    TO 'app_user'@'app-server.example.com';

-- Utilisateur en lecture seule
GRANT SELECT ON reporting_db.*
    TO 'bi_tool'@'%';
```

### Privilèges de structure (DDL)

| Privilège | Description | Risque | Production |
|-----------|-------------|--------|------------|
| **CREATE** | Créer tables/bases | Moyen | Limité |
| **ALTER** | Modifier structure | **Élevé** | DBA uniquement |
| **DROP** | Supprimer objets | **Critique** | DBA uniquement |
| **INDEX** | Gérer les index | Moyen | Dev/DBA |
| **CREATE TEMPORARY TABLES** | Tables temporaires | Faible | Applications |

```sql
-- Développeur avec droits DDL sur base de développement
GRANT CREATE, ALTER, DROP, INDEX ON dev_db.*
    TO 'developer'@'%';

-- Application avec tables temporaires uniquement
GRANT SELECT, INSERT, UPDATE, DELETE, CREATE TEMPORARY TABLES
    ON app_db.*
    TO 'app_user'@'%';
```

⚠️ **Attention Production** : En production, les privilèges `ALTER` et `DROP` ne devraient être accordés qu'aux DBA via des comptes nominatifs, jamais aux comptes applicatifs.

### Privilèges administratifs

| Privilège | Description | Niveau requis |
|-----------|-------------|---------------|
| **SUPER** | Opérations privilégiées | Super-admin |
| **RELOAD** | FLUSH, refresh | Admin |
| **SHUTDOWN** | Arrêter le serveur | Admin |
| **PROCESS** | Voir tous les processus | Admin/Monitoring |
| **REPLICATION SLAVE** | Réplication | Compte réplication |
| **REPLICATION CLIENT** | SHOW MASTER/SLAVE STATUS | Monitoring |
| **CREATE USER** | Gérer utilisateurs | Admin sécurité |
| **GRANT OPTION** | Déléguer privilèges | Admin délégué |

```sql
-- Administrateur de base de données
GRANT SUPER, RELOAD, PROCESS, CREATE USER
    ON *.*
    TO 'dba_admin'@'localhost';

-- Compte de monitoring
GRANT PROCESS, REPLICATION CLIENT
    ON *.*
    TO 'monitoring'@'monitoring-server.example.com';

-- Compte pour la réplication
GRANT REPLICATION SLAVE
    ON *.*
    TO 'replication_user'@'replica-server.example.com';
```

### 🆕 Privilèges granulaires MariaDB 11.8

MariaDB 11.8 introduit de nouveaux privilèges pour un contrôle plus fin :

| Privilège | Description | Nouveauté 11.8 |
|-----------|-------------|----------------|
| **READ_ONLY ADMIN** | Bypass read_only mode | 🆕 |
| **CONNECTION ADMIN** | Bypass max_connections | 🆕 |
| **BACKUP ADMIN** | Backup operations (BACKUP STAGE) | 🆕 |
| **BINLOG ADMIN** | Gérer binary logs | 🆕 |
| **BINLOG ENCRYPTION ADMIN** | Chiffrement binlog | 🆕 |
| **SLAVE MONITOR** | Monitoring réplication sans SUPER | 🆕 |

```sql
-- Séparation des privilèges administratifs (11.8+)
-- Compte de sauvegarde sans SUPER
GRANT BACKUP ADMIN, RELOAD, LOCK TABLES
    ON *.*
    TO 'backup_user'@'backup-server.example.com';

-- Administrateur read_only sans tous les droits SUPER
GRANT READ_ONLY ADMIN
    ON *.*
    TO 'maintenance_user'@'localhost';

-- Monitoring de réplication dédié
GRANT SLAVE MONITOR, REPLICATION CLIENT
    ON *.*
    TO 'repl_monitor'@'%';

-- Gestion des binary logs
GRANT BINLOG ADMIN
    ON *.*
    TO 'binlog_manager'@'localhost';
```

💡 **Meilleure pratique 11.8** : Utilisez les nouveaux privilèges granulaires au lieu de `SUPER` pour implémenter une vraie séparation des responsabilités (SoD - Separation of Duties).

---

## Privilèges spéciaux et options

### WITH GRANT OPTION

Permet au bénéficiaire de transférer ses privilèges à d'autres utilisateurs.

```sql
-- Administrateur de base avec délégation
GRANT ALL PRIVILEGES ON app_db.*
    TO 'db_admin'@'%'
    WITH GRANT OPTION;

-- L'administrateur peut ensuite déléguer :
-- (connecté en tant que db_admin)
GRANT SELECT, INSERT ON app_db.* TO 'new_user'@'%';
```

⚠️ **Attention** : `WITH GRANT OPTION` confère un pouvoir important. Ne l'accordez qu'aux administrateurs de confiance.

### Limitations de ressources

Contrôlez l'utilisation des ressources serveur par utilisateur :

```sql
-- Utilisateur avec quotas
GRANT SELECT ON reporting_db.*
    TO 'limited_user'@'%'
    MAX_QUERIES_PER_HOUR 1000
    MAX_UPDATES_PER_HOUR 100
    MAX_CONNECTIONS_PER_HOUR 50
    MAX_USER_CONNECTIONS 3;

-- Limitation du temps d'exécution des requêtes
GRANT SELECT ON analytics_db.*
    TO 'analyst'@'%'
    MAX_STATEMENT_TIME 300;  -- 5 minutes max
```

💡 **Conseil** : Les quotas sont stockés dans `mysql.user` et réinitialisés chaque heure. Utilisez-les pour protéger le serveur contre les utilisateurs abusifs.

---

## Cas d'usage et patterns de sécurité

### Pattern 1 : Application web standard

```sql
-- Compte applicatif avec moindre privilège
CREATE USER 'webapp'@'10.0.%.%'
    IDENTIFIED BY 'complex_password_here';

GRANT SELECT, INSERT, UPDATE, DELETE
    ON webapp_db.*
    TO 'webapp'@'10.0.%.%';

-- Table temporaire pour sessions
GRANT CREATE TEMPORARY TABLES
    ON webapp_db.*
    TO 'webapp'@'10.0.%.%';

-- Exécution de procédures métier uniquement
GRANT EXECUTE ON webapp_db.*
    TO 'webapp'@'10.0.%.%';
```

### Pattern 2 : Séparation lecture/écriture

```sql
-- Compte écriture (primary)
CREATE USER 'app_writer'@'app-servers%'
    IDENTIFIED BY 'writer_password';

GRANT SELECT, INSERT, UPDATE, DELETE
    ON app_db.*
    TO 'app_writer'@'app-servers%';

-- Compte lecture (replica)
CREATE USER 'app_reader'@'app-servers%'
    IDENTIFIED BY 'reader_password';

GRANT SELECT
    ON app_db.*
    TO 'app_reader'@'app-servers%';
```

### Pattern 3 : Développement vs Production

```sql
-- Développement : droits étendus
GRANT ALL PRIVILEGES ON dev_db.*
    TO 'developer'@'%';

-- Staging : droits limités
GRANT SELECT, INSERT, UPDATE, DELETE, CREATE TEMPORARY TABLES
    ON staging_db.*
    TO 'developer'@'%';

-- Production : aucun accès direct
-- Tous les changements via procédures de déploiement
```

### Pattern 4 : Masquage de données sensibles (colonnes)

```sql
-- Vue complète pour les RH
CREATE USER 'hr_full'@'%' IDENTIFIED BY 'password';
GRANT SELECT ON hr_db.employees TO 'hr_full'@'%';

-- Accès limité pour les managers (sans salaires)
CREATE USER 'manager'@'%' IDENTIFIED BY 'password';
GRANT SELECT (employee_id, first_name, last_name,
              department, hire_date, manager_id)
    ON hr_db.employees
    TO 'manager'@'%';

-- Alternative avec vue (recommandé)
CREATE VIEW hr_db.employees_public AS
    SELECT employee_id, first_name, last_name, department
    FROM hr_db.employees;

GRANT SELECT ON hr_db.employees_public TO 'manager'@'%';
```

### Pattern 5 : Compte de sauvegarde (MariaDB 11.8)

```sql
-- Utilisation des nouveaux privilèges granulaires 11.8
CREATE USER 'backup'@'localhost'
    IDENTIFIED BY 'backup_password';

-- Privilèges minimaux pour Mariabackup
GRANT BACKUP ADMIN, RELOAD, LOCK TABLES, PROCESS, REPLICATION CLIENT
    ON *.*
    TO 'backup'@'localhost';

-- Tables système nécessaires
GRANT SELECT ON mysql.* TO 'backup'@'localhost';
```

### Pattern 6 : Monitoring sans privilèges excessifs

```sql
-- Compte monitoring avec privilèges 11.8
CREATE USER 'monitoring'@'monitoring-server%'
    IDENTIFIED BY 'monitoring_password';

-- Privilèges de lecture des métriques
GRANT PROCESS, SLAVE MONITOR, REPLICATION CLIENT
    ON *.*
    TO 'monitoring'@'monitoring-server%';

-- Accès aux schemas système
GRANT SELECT ON performance_schema.*
    TO 'monitoring'@'monitoring-server%';

GRANT SELECT ON information_schema.*
    TO 'monitoring'@'monitoring-server%';
```

---

## Vérification des privilèges accordés

### Afficher les privilèges d'un utilisateur

```sql
-- Privilèges globaux et de base
SHOW GRANTS FOR 'username'@'hostname';

-- Privilèges de l'utilisateur courant
SHOW GRANTS;
SHOW GRANTS FOR CURRENT_USER;
SHOW GRANTS FOR CURRENT_USER();

-- Exemple de sortie :
-- GRANT SELECT, INSERT ON `app_db`.* TO `app_user`@`%`
-- GRANT EXECUTE ON PROCEDURE `app_db`.`process_order` TO `app_user`@`%`
```

### Interroger les tables système

```sql
-- Vue d'ensemble des privilèges globaux
SELECT user, host, Select_priv, Insert_priv, Update_priv,
       Delete_priv, Create_priv, Drop_priv
FROM mysql.user
WHERE user = 'app_user';

-- Privilèges de base de données
SELECT * FROM mysql.db
WHERE user = 'app_user' AND db = 'app_db';

-- Privilèges de tables
SELECT * FROM mysql.tables_priv
WHERE user = 'app_user';

-- Privilèges de colonnes
SELECT * FROM mysql.columns_priv
WHERE user = 'app_user';

-- 🆕 Vue consolidée (MariaDB 10.4+)
SELECT * FROM information_schema.user_privileges
WHERE grantee = "'app_user'@'%'";
```

---

## Rechargement des privilèges

Après modification des tables de privilèges, rechargez-les en mémoire :

```sql
-- Recharger les tables de privilèges
FLUSH PRIVILEGES;
```

💡 **Note** : `FLUSH PRIVILEGES` est **automatiquement exécuté** après `GRANT`, `REVOKE`, `CREATE USER`, etc. Il n'est nécessaire que si vous modifiez directement les tables `mysql.*` (pratique déconseillée).

---

## Bonnes pratiques de production

### 1. Principe du moindre privilège

```sql
-- ❌ MAUVAIS : Privilèges excessifs
GRANT ALL PRIVILEGES ON *.* TO 'app_user'@'%';

-- ✅ BON : Privilèges minimaux nécessaires
GRANT SELECT, INSERT, UPDATE, DELETE ON app_db.* TO 'app_user'@'%';
GRANT EXECUTE ON app_db.* TO 'app_user'@'%';
```

### 2. Séparation des comptes

```sql
-- ✅ Comptes séparés par fonction
-- Application
GRANT SELECT, INSERT, UPDATE, DELETE ON app_db.* TO 'app_prod'@'app%';

-- Lecture seule (reporting)
GRANT SELECT ON app_db.* TO 'app_readonly'@'reporting%';

-- Sauvegarde
GRANT BACKUP ADMIN, RELOAD ON *.* TO 'backup'@'localhost';

-- Monitoring
GRANT PROCESS, SLAVE MONITOR ON *.* TO 'monitor'@'monitoring%';
```

### 3. Restriction par host

```sql
-- ✅ Restreindre par origine réseau
GRANT SELECT ON app_db.* TO 'app_user'@'10.0.1.%';        -- Subnet spécifique
GRANT SELECT ON app_db.* TO 'app_user'@'app-server-%.example.com';  -- Pattern DNS

-- ❌ Éviter les wildcards trop larges
-- GRANT SELECT ON app_db.* TO 'app_user'@'%';  -- Trop permissif
```

### 4. Expiration et rotation des privilèges

```sql
-- 🆕 MariaDB 11.8 : Comptes avec expiration
CREATE USER 'temp_analyst'@'%'
    IDENTIFIED BY 'password'
    PASSWORD EXPIRE INTERVAL 90 DAY;

GRANT SELECT ON analytics_db.* TO 'temp_analyst'@'%';

-- Révocation automatisée via script pour les comptes temporaires
-- (à implémenter dans votre processus de gestion des accès)
```

### 5. Audit des privilèges

```sql
-- Script d'audit : utilisateurs avec privilèges globaux
SELECT user, host,
       Super_priv, Reload_priv, Shutdown_priv,
       Process_priv, Grant_priv
FROM mysql.user
WHERE Super_priv = 'Y'
   OR Grant_priv = 'Y'
ORDER BY user, host;

-- Utilisateurs avec accès à toutes les bases
SELECT user, host FROM mysql.user
WHERE Select_priv = 'Y' AND Delete_priv = 'Y'
  AND host = '%';
```

### 6. Documentation et traçabilité

```sql
-- Documenter l'attribution des privilèges
-- Utiliser des commentaires dans les scripts de création

-- 2025-12-13 : Création compte application e-commerce (ticket #1234)
CREATE USER 'ecommerce_app'@'10.20.30.%'
    IDENTIFIED BY 'secure_password';

GRANT SELECT, INSERT, UPDATE, DELETE ON ecommerce_db.*
    TO 'ecommerce_app'@'10.20.30.%';

-- Enregistrer dans un système de gestion de configuration (Git, Ansible)
```

### 7. Utiliser les rôles pour simplifier (voir section 10.4)

```sql
-- Définir des rôles plutôt que des privilèges individuels
CREATE ROLE app_reader;
GRANT SELECT ON app_db.* TO app_reader;

CREATE ROLE app_writer;
GRANT SELECT, INSERT, UPDATE, DELETE ON app_db.* TO app_writer;

-- Attribuer les rôles
GRANT app_reader TO 'user1'@'%';
GRANT app_writer TO 'user2'@'%';
```

---

## Erreurs courantes et dépannage

### Problème : Privilèges non appliqués

```sql
-- Symptôme : "Access denied" malgré GRANT

-- Vérification 1 : Privilèges accordés ?
SHOW GRANTS FOR 'user'@'host';

-- Vérification 2 : Host correspondant ?
SELECT user, host FROM mysql.user WHERE user = 'username';
-- Si l'utilisateur se connecte depuis '10.0.1.50' mais GRANT est pour 'localhost' → échec

-- Solution : GRANT avec le bon host
GRANT SELECT ON db.* TO 'user'@'10.0.1.50';
```

### Problème : WITH GRANT OPTION ne fonctionne pas

```sql
-- L'utilisateur ne peut pas déléguer ses privilèges

-- Cause : GRANT OPTION non accordé
-- Solution :
GRANT SELECT ON db.* TO 'user'@'%' WITH GRANT OPTION;
```

### Problème : Privilèges colonnes ignorés

```sql
-- Symptôme : SELECT * retourne toutes les colonnes malgré restriction

-- Cause : Privilège SELECT global ou database écrase les privilèges colonnes
SELECT user, host, Select_priv FROM mysql.user WHERE user = 'username';
SELECT user, host, Select_priv FROM mysql.db WHERE user = 'username';

-- Solution : Révoquer privilèges larges, n'accorder que les colonnes
REVOKE SELECT ON db.* FROM 'user'@'%';
GRANT SELECT (col1, col2) ON db.table TO 'user'@'%';
```

---

## Différences avec MySQL

| Fonctionnalité | MariaDB 11.8 | MySQL 8.0 |
|----------------|--------------|-----------|
| Privilèges granulaires | ✅ BACKUP ADMIN, BINLOG ADMIN, etc. | ✅ Similaires |
| Rôles | ✅ Depuis 10.0.5 | ✅ Depuis 8.0 |
| Authentication plugins | ✅ ed25519, PAM, LDAP, GSSAPI, PARSEC | Partiellement |
| DEFAULT ROLE | ✅ Automatique | ✅ Mais syntaxe différente |
| TLS par défaut | 🆕 ✅ Depuis 11.8 | ❌ Non par défaut |

---

## ✅ Points clés à retenir

- **GRANT** attribue des privilèges à 5 niveaux : global, base, table, colonne, routine
- Le **principe du moindre privilège** est fondamental : n'accordez que le strict nécessaire
- **MariaDB 11.8** introduit des privilèges granulaires (`BACKUP ADMIN`, `BINLOG ADMIN`, etc.) pour remplacer l'usage abusif de `SUPER`
- Utilisez **WITH GRANT OPTION** avec parcimonie (délégation de privilèges)
- Les **quotas de ressources** (`MAX_QUERIES_PER_HOUR`, etc.) protègent le serveur
- **Restreignez par host** : évitez `'%'` en production, utilisez des subnets ou DNS spécifiques
- **Séparez les comptes** par fonction (app, readonly, backup, monitoring)
- **Auditez régulièrement** les privilèges accordés via les tables `mysql.*` et `information_schema`

---

## 🔗 Ressources et références

- [📖 Documentation officielle MariaDB - GRANT](https://mariadb.com/kb/en/grant/)
- [📖 Privilege System Overview](https://mariadb.com/kb/en/grant/#privilege-levels)
- [📖 MariaDB 11.8 Release Notes - Nouveaux privilèges](https://mariadb.com/kb/en/mariadb-1180-release-notes/)
- [📖 Account Management Statements](https://mariadb.com/kb/en/account-management-sql-commands/)
- [Blog : Implementing Least Privilege in MariaDB](https://mariadb.org/least-privilege-best-practices/)

---

## ➡️ Section suivante

**10.3.2 [REVOKE : Révocation de privilèges](/10-securite-gestion-utilisateurs/03.2-revoke.md)** : Apprenez à retirer des privilèges de manière sécurisée, gérer les dépendances et auditer les révocations.

⏭️ [REVOKE : Révocation de privilèges](/10-securite-gestion-utilisateurs/03.2-revoke.md)

🔝 Retour au [Sommaire](/SOMMAIRE.md)

# 10.5.1 mysql_native_password

> **Niveau** : Avancé
> **Durée estimée** : 1 heure
> **Prérequis** : Section 10.2 (Création et gestion des utilisateurs)

## 🎯 Objectifs d'apprentissage

À l'issue de cette section, vous serez capable de :
- Comprendre le fonctionnement technique de mysql_native_password
- Identifier les forces et faiblesses de ce plugin d'authentification
- Configurer et gérer les utilisateurs avec mysql_native_password
- Évaluer quand utiliser (ou éviter) ce plugin en production
- Planifier une migration vers des alternatives plus sécurisées

---

## Introduction

`mysql_native_password` est le plugin d'authentification **historique** de MySQL et MariaDB, utilisé par défaut pendant plus de 15 ans. Bien qu'il soit toujours largement supporté pour des raisons de **compatibilité rétroactive**, il est considéré comme **legacy** depuis l'émergence de méthodes d'authentification plus robustes.

En MariaDB 11.8, ce plugin reste disponible mais **n'est plus recommandé** pour les nouveaux déploiements. Les administrateurs sont encouragés à utiliser des alternatives modernes comme `ed25519` ou `unix_socket` (selon le cas d'usage).

⚠️ **État actuel** : Plugin legacy maintenu pour compatibilité. **Non recommandé** pour les nouveaux systèmes.

---

## Fonctionnement technique

### Architecture du challenge-response

`mysql_native_password` implémente un mécanisme **challenge-response** pour éviter la transmission du mot de passe en clair sur le réseau.

```
┌──────────┐                                    ┌──────────┐
│  Client  │                                    │  Serveur │
└─────┬────┘                                    └────┬─────┘
      │                                              │
      │  1. Demande de connexion                     │
      │─────────────────────────────────────────────>│
      │                                              │
      │  2. Envoie salt (20 octets aléatoires)       │
      │<─────────────────────────────────────────────│
      │                                              │
      │  3. Hash = SHA1(                             │
      │        SHA1(password) XOR                    │
      │        SHA1(                                 │
      │          SHA1(SHA1(password)) + salt         │
      │        )                                     │
      │     )                                        │
      │                                              │
      │  4. Envoie hash calculé                      │
      │─────────────────────────────────────────────>│
      │                                              │
      │                     5. Serveur compare       │
      │                        hash reçu avec hash   │
      │                        stocké                │
      │                                              │
      │  6. Authentification OK/KO                   │
      │<─────────────────────────────────────────────│
      │                                              │
```

### Algorithme de hachage

```python
# Pseudocode du processus
password = "mon_password"

# Étape 1 : Double SHA1 du mot de passe (stocké dans mysql.user)
stage1 = SHA1(password)
stage2 = SHA1(stage1)  # Stocké dans mysql.user

# Lors de l'authentification (challenge-response)
salt = random_bytes(20)  # Généré par le serveur

# Client calcule
client_hash = SHA1(stage1 XOR SHA1(stage2 + salt))

# Serveur calcule (avec stage2 stocké en base)
server_hash = SHA1(SHA1(stage2) XOR SHA1(stage2 + salt))

# Comparaison
if client_hash == server_hash:
    authenticate_success()
```

### Format de stockage

```sql
-- Visualiser le hash stocké
SELECT user, host, authentication_string
FROM mysql.user
WHERE plugin = 'mysql_native_password';

-- Exemple de résultat :
-- user: 'app_user'
-- host: '%'
-- authentication_string: '*6BB4837EB74329105EE4568DDA7DC67ED2CA2AD9'
--                        ↑
--                        41 caractères (1 '*' + 40 hex = SHA1(SHA1(password)))
```

Le hash est préfixé par `*` et contient 40 caractères hexadécimaux (160 bits = SHA1).

---

## Configuration et utilisation

### Création d'utilisateur avec mysql_native_password

```sql
-- Méthode explicite (recommandée pour la clarté)
CREATE USER 'app_user'@'%'
    IDENTIFIED VIA mysql_native_password
    USING PASSWORD('SecureP@ssw0rd123');

-- Méthode alternative (syntaxe historique)
CREATE USER 'app_user'@'%'
    IDENTIFIED BY 'SecureP@ssw0rd123';
-- Si default_authentication_plugin = mysql_native_password

-- Avec hash pré-calculé (utile pour migrations)
CREATE USER 'app_user'@'%'
    IDENTIFIED VIA mysql_native_password
    USING '*6BB4837EB74329105EE4568DDA7DC67ED2CA2AD9';
```

### Vérifier le plugin d'authentification

```sql
-- Pour un utilisateur spécifique
SELECT user, host, plugin, authentication_string
FROM mysql.user
WHERE user = 'app_user';

-- Tous les utilisateurs avec mysql_native_password
SELECT user, host, plugin
FROM mysql.user
WHERE plugin = 'mysql_native_password'
ORDER BY user, host;

-- Statistiques par plugin
SELECT plugin, COUNT(*) AS user_count
FROM mysql.user
GROUP BY plugin
ORDER BY user_count DESC;
```

### Modifier un utilisateur existant

```sql
-- Changer le plugin vers mysql_native_password
ALTER USER 'existing_user'@'%'
    IDENTIFIED VIA mysql_native_password
    USING PASSWORD('NewPassword123');

-- Changer uniquement le mot de passe (conserver le plugin)
ALTER USER 'app_user'@'%'
    IDENTIFIED BY 'NewPassword456';
-- Le plugin reste mysql_native_password si c'était déjà le cas
```

### Configuration serveur

```ini
# my.cnf / mariadb.cnf
[mysqld]
# Plugin par défaut pour CREATE USER (legacy)
default_authentication_plugin = mysql_native_password

# OU avec la syntaxe MariaDB moderne
# default-authentication-plugin = mysql_native_password
```

⚠️ **Attention** : Depuis MariaDB 10.4+, le plugin par défaut est **mysql_native_password** par compatibilité, mais cela pourrait changer dans les futures versions majeures.

---

## Forces et faiblesses

### ✅ Avantages

| Avantage | Description | Impact |
|----------|-------------|--------|
| **Compatibilité maximale** | Supporté par tous les clients MySQL/MariaDB depuis 20 ans | Pas de problèmes de connexion |
| **Simplicité** | Pas de configuration système (PAM, LDAP, etc.) | Facile à déployer |
| **Performance** | Hash SHA1 très rapide (CPU minimal) | Pas d'overhead |
| **Portable** | Fonctionne sur toutes les plateformes | Windows, Linux, macOS, etc. |
| **Challenge-response** | Mot de passe jamais transmis en clair | Sécurité de base |

### ❌ Inconvénients (critiques pour la sécurité)

| Faiblesse | Description | Risque |
|-----------|-------------|--------|
| **SHA1 obsolète** | Algorithme cryptographiquement cassé depuis 2017 | **Élevé** |
| **Collision SHA1** | Possibilité de générer des collisions | **Moyen** |
| **Pas de salt par utilisateur** | Salt unique par session, pas par utilisateur | **Moyen** |
| **Vitesse de brute-force** | SHA1 très rapide = milliards de hash/seconde | **Élevé** |
| **Pas de stretching** | Un seul double-SHA1, pas d'itérations | **Élevé** |
| **Vulnérable aux rainbow tables** | Hash prévisible sans salt persistant | **Moyen** |

💡 **Contexte SHA1** : En février 2017, Google a démontré une collision SHA1 pratique ([SHAttered attack](https://shattered.io/)). Bien que cela ne compromette pas directement mysql_native_password, cela démontre la faiblesse de l'algorithme.

---

## Sécurité et vulnérabilités

### 1. Vitesse de brute-force

Avec du matériel moderne (GPU), un attaquant peut tester des **milliards de mots de passe par seconde** contre un hash SHA1.

```bash
# Exemple avec hashcat (outil de cracking)
# Format MariaDB/MySQL native password : mode 300
hashcat -m 300 -a 0 hash.txt wordlist.txt

# Performance typique (GPU moderne) :
# - NVIDIA RTX 4090 : ~100 GH/s (100 milliards de hash/seconde)
# - Mot de passe "Password123" : craqué en < 1 seconde
```

⚠️ **Impact** : Un mot de passe faible (< 12 caractères, dictionnaire) peut être craqué en minutes/heures si l'attaquant obtient le hash.

### 2. Attaque sur dump de base

```bash
# Si un attaquant obtient un dump de mysql.user
mysqldump -u root -p mysql user > dump.sql

# Contenu du dump :
# INSERT INTO `user` VALUES ('app_user','%','*6BB4837...',...);

# L'attaquant peut extraire les hash et lancer un brute-force offline
grep authentication_string dump.sql | grep mysql_native_password
```

💡 **Mitigation** :
- Mots de passe longs et complexes (16+ caractères)
- Surveillance des dumps de base (DLP)
- Chiffrement at-rest de `mysql.user`

### 3. Absence de protection contre replay (sans SSL)

Sans SSL/TLS, bien que le mot de passe ne soit pas transmis en clair, un attaquant qui intercepte le trafic peut capturer le **challenge et la réponse**.

```
Attaquant en man-in-the-middle :
1. Capture salt + hash du client
2. Peut rejouer l'authentification si le salt est réutilisé
   (peu probable, salt aléatoire par session)
```

⚠️ **Mitigation obligatoire** : Toujours utiliser **SSL/TLS** pour les connexions réseau (voir section 10.7).

### 4. Pas de protection contre les attaques par dictionnaire

```sql
-- Aucune limitation native de tentatives
-- Un attaquant peut tenter des milliers de connexions
mysql -u app_user -p'password1'  # Échec
mysql -u app_user -p'password2'  # Échec
mysql -u app_user -p'Password123'  # Succès après N tentatives
```

💡 **Mitigation** : Utiliser `max_connect_errors`, fail2ban, ou un pare-feu applicatif.

---

## Comparaison avec les alternatives modernes

| Critère | mysql_native_password | ed25519 | PAM | unix_socket |
|---------|------------------------|---------|-----|-------------|
| **Algorithme** | SHA1 (cassé) | Ed25519 (robuste) | Système OS | Socket Unix |
| **Résistance brute-force** | ❌ Faible | ✅ Excellente | ✅ Selon config | N/A |
| **Compatibilité** | ✅✅✅ Maximale | ⚠️ Moderne | ⚠️ OS-spécifique | ⚠️ Linux local |
| **Performance** | ✅ Rapide | ✅ Rapide | ⚠️ Variable | ✅ Rapide |
| **Configuration** | ✅ Simple | ✅ Simple | ⚠️ Complexe | ✅ Simple |
| **Sécurité 2025** | ❌ Legacy | ✅ Recommandé | ✅ Recommandé | ✅ Recommandé |

---

## Cas d'usage légitimes (2025)

Malgré ses faiblesses, `mysql_native_password` reste pertinent dans certains contextes :

### 1. Compatibilité avec anciens clients

```sql
-- Application legacy qui ne supporte que mysql_native_password
-- (ex: ancienne version de PHP, bibliothèques 2010-2015)
CREATE USER 'legacy_app'@'old-server%'
    IDENTIFIED VIA mysql_native_password
    USING PASSWORD('ComplexLegacyP@ss123');

-- Documenter la raison
COMMENT ON USER 'legacy_app'@'old-server%'
    IS 'Legacy app - migration to ed25519 planned Q2 2026';
```

### 2. Migration progressive

```sql
-- Phase de transition : nouveaux utilisateurs en ed25519
CREATE USER 'new_user'@'%'
    IDENTIFIED VIA ed25519
    USING PASSWORD('SecurePassword123');

-- Utilisateurs existants conservent mysql_native_password temporairement
-- Migration planifiée
```

### 3. Environnements de développement/test

```sql
-- Dev/test où la sécurité n'est pas critique
-- (jamais en production !)
CREATE USER 'dev_user'@'localhost'
    IDENTIFIED VIA mysql_native_password
    USING PASSWORD('dev123');
```

⚠️ **Avertissement** : Même en dev/test, privilégiez de bonnes pratiques pour éviter les mauvaises habitudes.

---

## Bonnes pratiques avec mysql_native_password

Si vous devez utiliser ce plugin (transitoirement), suivez ces règles :

### 1. Mots de passe robustes obligatoires

```sql
-- ❌ MAUVAIS : Mot de passe faible
CREATE USER 'app'@'%' IDENTIFIED BY 'password123';

-- ✅ BON : Mot de passe fort (16+ caractères, complexe)
CREATE USER 'app'@'%'
    IDENTIFIED VIA mysql_native_password
    USING PASSWORD('Xy9$mKp2#Qw8@Zn5!Rt7');

-- Utiliser un générateur de mots de passe
-- openssl rand -base64 24
-- ou pwgen -s 20
```

### 2. Politique de mots de passe stricte

```sql
-- Activer le plugin de validation
INSTALL SONAME 'simple_password_check';

-- Configuration
SET GLOBAL simple_password_check_minimal_length = 16;
SET GLOBAL simple_password_check_digits = 2;
SET GLOBAL simple_password_check_letters_same_case = 2;
SET GLOBAL simple_password_check_other_characters = 2;

-- Tester
CREATE USER 'test'@'%' IDENTIFIED BY 'weak';
-- ERROR 1819 (HY000): Your password does not satisfy the current policy requirements
```

### 3. Expiration régulière des mots de passe

```sql
-- Politique d'expiration (MariaDB 10.4+)
CREATE USER 'app_user'@'%'
    IDENTIFIED VIA mysql_native_password
    USING PASSWORD('InitialP@ss123')
    PASSWORD EXPIRE INTERVAL 90 DAY;

-- Vérifier les comptes expirés
SELECT user, host,
       password_expired,
       password_lifetime,
       password_last_changed
FROM mysql.user
WHERE plugin = 'mysql_native_password'
  AND password_expired = 'Y';
```

### 4. SSL/TLS obligatoire

```sql
-- Forcer SSL pour les connexions réseau
CREATE USER 'app_user'@'remote-host%'
    IDENTIFIED VIA mysql_native_password
    USING PASSWORD('SecureP@ss123')
    REQUIRE SSL;

-- Vérifier
SELECT user, host, ssl_type
FROM mysql.user
WHERE user = 'app_user';
```

### 5. Audit et monitoring

```sql
-- Activer l'audit des connexions
INSTALL SONAME 'server_audit';

SET GLOBAL server_audit_logging = ON;
SET GLOBAL server_audit_events = 'CONNECT,FAILED_CONNECT';

-- Surveiller les échecs d'authentification
SELECT argument
FROM mysql.general_log
WHERE command_type = 'Connect'
  AND argument LIKE '%Access denied%'
ORDER BY event_time DESC
LIMIT 20;
```

### 6. Limitation des tentatives

```ini
# my.cnf
[mysqld]
# Bloquer après 10 erreurs de connexion
max_connect_errors = 10

# Timeout de connexion court
connect_timeout = 10
```

---

## Migration vers ed25519

### Stratégie de migration

```sql
-- 1. Identifier tous les utilisateurs mysql_native_password
SELECT user, host, plugin,
       CONCAT('ALTER USER ''', user, '''@''', host,
              ''' IDENTIFIED VIA ed25519 USING PASSWORD(''...'');')
       AS migration_sql
FROM mysql.user
WHERE plugin = 'mysql_native_password'
  AND user NOT IN ('root', 'mariadb.sys');

-- 2. Migration par phases
-- Phase 1 : Utilisateurs non-critiques
ALTER USER 'reporting_user'@'%'
    IDENTIFIED VIA ed25519
    USING PASSWORD('NewEdP@ss123');

-- Phase 2 : Applications (avec mise à jour drivers)
-- Vérifier compatibilité client avant migration

-- Phase 3 : Comptes administratifs
ALTER USER 'dba_admin'@'localhost'
    IDENTIFIED VIA ed25519
    USING PASSWORD('AdminEdP@ss456');
```

### Vérification de compatibilité client

```bash
# Vérifier si le client supporte ed25519
mysql --version

# MySQL client 8.0.19+ : ✅ Support ed25519
# MariaDB client 10.2+ : ✅ Support ed25519
# MySQL client < 8.0.19 : ❌ Pas de support

# Test de connexion
mysql -u test_user -p -h server --ssl-mode=REQUIRED
```

### Rollback si nécessaire

```sql
-- En cas de problème, rollback temporaire
ALTER USER 'app_user'@'%'
    IDENTIFIED VIA mysql_native_password
    USING PASSWORD('TemporaryFallbackP@ss');

-- Documenter l'incident
INSERT INTO security_audit.auth_migrations
(user, host, action, reason, timestamp)
VALUES
('app_user', '%', 'ROLLBACK_TO_NATIVE',
 'Client incompatibility - old PHP version', NOW());
```

---

## Dépréciation et futur

### État actuel (MariaDB 11.8)

- ✅ **Supporté** : Pleinement fonctionnel
- ⚠️ **Legacy** : Considéré obsolète
- 📝 **Documentation** : Recommande ed25519 pour nouveaux déploiements

### Évolution prévue

```
MariaDB 10.x - 11.x : mysql_native_password par défaut (compatibilité)
MariaDB 12.x (2026) : Possible changement du plugin par défaut vers ed25519
MariaDB 13.x+ (2027) : Possible dépréciation formelle avec warnings
MariaDB 14.x+ (2028+) : Possible retrait (mais peu probable avant 2030)
```

💡 **Recommandation** : Planifiez dès maintenant la migration vers ed25519 pour éviter une migration forcée future.

---

## Audit et conformité

### Détecter l'utilisation de mysql_native_password

```sql
-- Script d'audit de sécurité
CREATE OR REPLACE VIEW security_audit.legacy_auth_users AS
SELECT
    u.user,
    u.host,
    u.plugin,
    'mysql_native_password is LEGACY - migrate to ed25519' AS recommendation,
    CASE
        WHEN u.user IN ('root', 'mariadb.sys') THEN 'SYSTEM'
        WHEN u.host = 'localhost' THEN 'LOW_RISK'
        WHEN u.host LIKE '10.%' OR u.host LIKE '192.168.%' THEN 'MEDIUM_RISK'
        ELSE 'HIGH_RISK'
    END AS risk_level
FROM mysql.user u
WHERE u.plugin = 'mysql_native_password'
ORDER BY
    CASE risk_level
        WHEN 'HIGH_RISK' THEN 1
        WHEN 'MEDIUM_RISK' THEN 2
        WHEN 'LOW_RISK' THEN 3
        WHEN 'SYSTEM' THEN 4
    END;

-- Utilisation
SELECT * FROM security_audit.legacy_auth_users;
```

### Rapport de conformité

```sql
-- Pourcentage d'utilisateurs avec authentification moderne
SELECT
    COUNT(*) AS total_users,
    SUM(CASE WHEN plugin = 'mysql_native_password' THEN 1 ELSE 0 END) AS legacy_auth,
    SUM(CASE WHEN plugin IN ('ed25519', 'unix_socket', 'pam') THEN 1 ELSE 0 END) AS modern_auth,
    ROUND(100.0 * SUM(CASE WHEN plugin IN ('ed25519', 'unix_socket', 'pam') THEN 1 ELSE 0 END) / COUNT(*), 2)
        AS modern_auth_percentage
FROM mysql.user
WHERE user NOT IN ('root', 'mariadb.sys', '');
```

---

## ✅ Points clés à retenir

- `mysql_native_password` est le plugin d'authentification **legacy** de MySQL/MariaDB
- Utilise **SHA1** (cassé en 2017) avec un mécanisme challenge-response
- **Vulnérable au brute-force** : mots de passe faibles craquables en minutes
- **Compatibilité maximale** : tous les clients MySQL/MariaDB depuis 20 ans
- **Non recommandé en 2025** pour nouveaux déploiements : préférer **ed25519**
- Si utilisation obligatoire : **mots de passe forts (16+ car.)**, **SSL/TLS obligatoire**, **expiration régulière**
- **Planifier la migration** vers ed25519 pour éviter une transition forcée future
- **Auditer régulièrement** les utilisateurs avec ce plugin et prioriser la migration

---

## 🔗 Ressources et références

- [📖 Documentation officielle - mysql_native_password](https://mariadb.com/kb/en/authentication-plugin-mysql_native_password/)
- [📖 Password Validation Plugins](https://mariadb.com/kb/en/password-validation-plugins/)
- [📖 SHAttered: SHA1 collision attack (2017)](https://shattered.io/)
- [📖 Migration guide to ed25519](https://mariadb.com/kb/en/authentication-plugin-ed25519/)
- [Blog : Why SHA1 is deprecated for password hashing](https://mariadb.org/password-security-best-practices/)

---

## ➡️ Section suivante

**10.5.2 [ed25519 : Authentification moderne](/10-securite-gestion-utilisateurs/05.2-ed25519.md)** : Découvrez le plugin d'authentification recommandé pour MariaDB, basé sur la cryptographie à courbes elliptiques, résistant au brute-force et aux attaques modernes.

⏭️ [ed25519 : Authentification moderne](/10-securite-gestion-utilisateurs/05.2-ed25519.md)
